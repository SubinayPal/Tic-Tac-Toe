import { TempconvertPipe } from './tempconvert.pipe';

describe('TempconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new TempconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
